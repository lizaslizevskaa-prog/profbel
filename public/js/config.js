// API base URL — all frontend fetch calls should use this constant.
// When running locally (same origin), leave empty string. For cross-origin
// deployment, set to the backend URL.
const API_URL = "https://profbel-production.up.railway.app";
